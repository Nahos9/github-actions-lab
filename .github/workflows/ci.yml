name: Mon premier CI


on:
  push:
    branchs:
       -main

jobs:
  test:
   runs-on: ubuntu-latest

  steps:
   -name: Checkout
    uses: actions/checkout@v4
 
   -name: Vérifier le système
    run: |
       echo "Pipeline GiHub Actions"
       echo "Utilisateur : $USER"
       echo  "OS : "
       uname -a
  
   -name: Tester
    run: echo "Les tests sont OK"
